# English Listening Gym — Docker 部署方案

## 概述

将英语听力训练应用以容器化方式部署到 **Ubuntu 24.04 LTS** 服务器。

| 组件 | 技术 | 端口 |
|------|------|------|
| 后端 | FastAPI + Uvicorn (Python 3.11) | `8000` |
| 前端 | Vue 3 + Vite (构建后由 Nginx 托管) | `80` |
| 数据持久化 | Docker Volume | `english_listening_data` |
| LLM | 智谱 GLM (在线 API) | — |
| TTS | Edge-TTS (在线服务) | — |

---

## 架构

```
                          ┌──────────┐
  Browser ──► :80 ──► Nginx (frontend)
                          │  /api/*  │
                          ▼          │
                       backend:8000  │
                          │          │
                     FastAPI + edge-tts + ChromaDB
                          │
                     Volume: data (SQLite / audio / Chroma)
```

- 前端 SPA 静态文件由 **Nginx** 直接托管，性能最佳
- `/api/*` 请求反向代理到后端容器
- 后端数据（SQLite、音频文件、向量库）通过 **Docker Volume** 持久化，重启不丢失
- LLM 和 TTS 均使用在线 API，容器需能访问公网

---

## 前提条件（服务器）

- Ubuntu 24.04 LTS
- root 或 sudo 权限
- 公网访问（LLM API / Edge-TTS）
- （可选）域名指向服务器 IP

---

## 快速部署（一键脚本）

```bash
# 1. 克隆或上传项目到服务器
git clone https://github.com/TslldBaoGe/english_listening_gym.git
cd english_listening_gym

# 2. 安装 Docker（如未安装）
sudo bash deploy/install-docker.sh
# 按提示登出再登入（或执行 newgrp docker）

# 3. 一键部署（不需要 .env，API Key 在网页设置页面配）
bash deploy/deploy.sh
```

部署完成后：
1. 浏览器访问 `http://<服务器IP>`
2. 进入「设置」页面 → **模型服务** 区域
3. 选择服务商、填写 API Key、选择模型 → 保存 + 测试连接
4. 开始练习！🎉

> ⚠️ 部署脚本中的 `.env` 检查已移除——**所有 LLM 配置都在网页设置页面里完成**，不经过环境变量。

---

## 手动分步部署

### 1. 安装 Docker + Compose

```bash
# 卸载旧版
for pkg in docker.io docker-doc docker-compose docker-compose-v2 podman-docker containerd runc; do
  sudo apt-get remove -y $pkg
done

# 安装依赖
sudo apt-get update
sudo apt-get install -y ca-certificates curl

# 添加 Docker 官方源
sudo install -m 0755 -d /etc/apt/keyrings
sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
sudo chmod a+r /etc/apt/keyrings/docker.asc
echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu noble stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安装
sudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# 当前用户加入 docker 组（免 sudo）
sudo usermod -aG docker $USER
newgrp docker   # 或登出重新登录
```

### 2. 构建并启动（无需 .env 文件）

> ℹ️ **API Key 和模型配置全部在网页设置页面中完成**，不需要提前配置环境变量。
> 部署后用浏览器访问服务器 IP → 进入「设置」→ 模型服务 → 填入 API Key 即可。
> 
> 只有当你希望「数据库尚未保存任何模型配置」时用**不同的默认模型**，
> 才需要设置 `LLM_MODEL` 环境变量（默认 `glm-4-flash`）。

```bash
# 可有可无的环境变量（仅用于默认模型回退值）
export LLM_MODEL=glm-4-flash
```

### 3. 构建并启动

```bash
# 构建镜像（首次约 5-10 分钟）
docker compose build

# 启动服务（后台）
docker compose up -d

# 查看日志
docker compose logs -f

# 查看状态
docker compose ps
```

### 4. 验证部署

```bash
# 前端
curl -s -o /dev/null -w "%{http_code}" http://localhost
# 应返回 200

# 后端 API
curl -s http://localhost:8000/api/meta | head -c 200
# 应返回 JSON 元数据
```

---

## 首次部署后 ⭐

部署成功后，打开浏览器访问 `http://<服务器IP>`，然后：

1. 点击导航栏 **「设置」**
2. 在 **「模型服务」** 区域中选择 LLM 服务商（如智谱、DeepSeek、OpenAI 等）
3. 填入你的 **API Key** 和 **Base URL**
4. 点击 **「获取模型」** 下拉选择可用模型
5. 点击 **「测试连接」** 验证配置是否正确
6. 点击 **「保存配置」**
7. 回到练习页，开始使用！🎉

> API Key 保存在容器内的 SQLite 数据库（Docker Volume）中，重启容器不会丢失。

---

## 服务管理

```bash
# 启动
docker compose up -d

# 停止
docker compose down

# 重启
docker compose restart

# 查看日志
docker compose logs -f          # 所有服务
docker compose logs -f backend  # 仅后端
docker compose logs -f frontend # 仅前端

# 重建镜像（更新代码后）
docker compose build
docker compose up -d
```

---

## 数据持久化

数据存储在 Docker 命名卷 `english_listening_data` 中，包括：

| 路径 | 内容 |
|------|------|
| `/app/data/app.db` | SQLite 数据库（句子、测验记录、设置） |
| `/app/data/audio/*.mp3` | 生成的音频文件 |
| `/app/data/chroma/` | ChromaDB 向量库（句子去重） |

```bash
# 备份数据
docker run --rm -v english_listening_data:/data -v $(pwd):/backup alpine tar czf /backup/el-data-$(date +%Y%m%d).tar.gz -C /data .

# 恢复数据
docker run --rm -v english_listening_data:/data -v $(pwd):/backup alpine tar xzf /backup/el-data-20250101.tar.gz -C /data
```

---

## 安全建议

1. **API Key 保护**：通过「设置」页面配置的 API Key 保存在本地数据库（`app.db`）中，Docker Volume  持久化，不写入代码。首次配好后重启不会丢失
2. **防火墙**：仅开放端口 `80`（前端），`8000` 不需要对外开放
3. **反向代理 HTTPS**（推荐）：

```bash
# 使用 Caddy（自动 HTTPS）
sudo apt-get install -y caddy
# /etc/caddy/Caddyfile:
# your-domain.com {
#     reverse_proxy localhost:80
# }

# 或使用 Nginx + certbot
```

4. **数据库备份**：定期备份 `english_listening_data` 卷

---

## 常见问题

**Q: 启动后访问不了？**

```bash
# 检查容器状态
docker compose ps
# 检查日志
docker compose logs backend
docker compose logs frontend
# 检查防火墙
sudo ufw status
sudo ufw allow 80/tcp
```

**Q: 前端报 502 Bad Gateway？**

后端尚未就绪，等待 10-15 秒后刷新。或检查后端日志：
```bash
docker compose logs backend
```

**Q: 如何更新代码？**

```bash
git pull
docker compose build
docker compose up -d
```

**Q: Edge-TTS 在容器内发声吗？**

Edge-TTS 调用的是微软在线 API（`speech.microsoft.com`），容器内只需能出网即可，无需声卡设备。

---

## 文件清单

| 文件 | 说明 |
|------|------|
| `docker-compose.yml` | Docker Compose 编排 |
| `Dockerfile.backend` | 后端镜像构建 |
| `Dockerfile.frontend` | 前端镜像构建（多阶段） |
| `nginx.conf` | Nginx 反向代理配置 |
| `.env.example` | 环境变量模板 |
| `.dockerignore` | Docker 构建忽略列表 |
| `deploy/install-docker.sh` | Docker 安装脚本（Ubuntu 24） |
| `deploy/deploy.sh` | 一键部署脚本 |
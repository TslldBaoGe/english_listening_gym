# English Listening Gym — Environment Variables (可选)
# 所有 LLM 配置（API Key / Base URL / 模型）均可通过网页设置页面配置，
# 保存到本地数据库，无需环境变量。
#
# 只有当你希望「数据库尚未保存任何模型」时用一个不同的默认模型，
# 才需要设置 LLM_MODEL。

# 可选：默认 LLM 模型（数据库无记录时的回退值，默认 glm-4-flash）
# LLM_MODEL=glm-4-flash